# === CONFIGURATION UNIFIÉE DIRAVENIR ===
# Un seul fichier pour tous les environnements !

# === PROFIL ACTIF ===
spring.profiles.active=dev

# === BASE DE DONNÉES MYSQL ===
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:diravenir}?useSSL=${DB_USE_SSL:false}&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === JPA/HIBERNATE ===
# Mode UPDATE pour le développement, VALIDATE pour la production
spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${HIBERNATE_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${HIBERNATE_FORMAT_SQL:true}

# === PERFORMANCE JPA ===
spring.jpa.open-in-view=false

# === JWT CONFIGURATION ===
# Configuration JWT unifiée (voir section JWT plus bas)

# === FLYWAY ===
# Temporairement désactivé pour résoudre le conflit de migration
spring.flyway.enabled=${FLYWAY_ENABLED:false}
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true

# === EMAIL CONFIGURATION ===
# IMPORTANT: Configuration Gmail avec vos vraies informations
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:imanecompte024@gmail.com}
spring.mail.password=${MAIL_PASSWORD:eeor guik iftz nico}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# === OAUTH2 GOOGLE CONFIGURATION ===
spring.security.oauth2.client.registration.google.client-id=1037870342905-b37d3kenk6qu0j67d1pmt6b7gufi9rht.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
spring.security.oauth2.client.registration.google.client-authentication-method=client_secret_post
spring.security.oauth2.client.registration.google.authorization-grant-type=authorization_code

# === JWT ===
jwt.secret=${JWT_SECRET:your-super-secure-jwt-secret-key-256-bits-minimum-required-for-hs512-algorithm-make-it-very-long-and-random}
jwt.expiration=${JWT_EXPIRATION:86400000}
app.jwt.refresh-expiration-ms=${JWT_REFRESH_EXPIRATION:604800000}

# === SERVER ===
server.port=${SERVER_PORT:8084}
server.servlet.context-path=/

# === APP CONFIG ===
app.frontend.url=${FRONTEND_URL:http://localhost:3000}
app.rate-limit.max-requests-per-minute=${RATE_LIMIT_MAX:60}
app.rate-limit.max-signup-per-hour=${SIGNUP_RATE_LIMIT:10}

# === LOGGING ===
logging.level.com.dira.diravenir1=${LOG_LEVEL:DEBUG}
logging.level.org.springframework.security=${LOG_LEVEL:DEBUG}
logging.level.org.springframework.web=${LOG_LEVEL:DEBUG}
logging.level.org.springframework.mail=${LOG_LEVEL:DEBUG}
logging.level.org.hibernate.SQL=${LOG_LEVEL:DEBUG}
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=${LOG_LEVEL:TRACE}
logging.level.org.flyway=${LOG_LEVEL:DEBUG}
