spring.application.name=diravenir1
# Connexion à la base MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/diravenir_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Meriem5527

# Configuration JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Console H2 désactivée (tu utilises MySQL)
spring.h2.console.enabled=false

# Niveau de log (optionnel pour debug)
logging.level.org.hibernate.SQL=DEBUG
