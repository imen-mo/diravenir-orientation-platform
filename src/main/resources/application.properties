# ======================
# === MySQL CONFIG  ===
# ======================
spring.datasource.url=jdbc:mysql://localhost:3306/diravenir?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=false

# ======================
# === JWT CONFIG     ===
# ======================
# ======================
# === CORS CONFIG    ===
# ======================
spring.web.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:5173,http://localhost:5174}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# ======================
# === RECAPTCHA      ===
# ======================
google.recaptcha.secret=6Lf6Vp0rAAAAAOb1UgOPdu9VHl3uVj9tb0TClXPt
google.recaptcha.site=6Lf6Vp0rAAAAAMghRpLjSbffcSEF7Z-JGBZbZA0U
google.recaptcha.url=https://www.google.com/recaptcha/api/siteverify

# ======================
# === ENVIRONMENT    ===
# ======================
app.environment=${APP_ENVIRONMENT:dev}

# ======================
# === SECURITY       ===
# ======================
# Rate limiting
app.rate-limit.max-requests-per-minute=${RATE_LIMIT_MAX:60}
app.rate-limit.max-signup-per-hour=${SIGNUP_RATE_LIMIT:10}

# Password policy
app.password.min-length=${PASSWORD_MIN_LENGTH:8}
app.password.require-uppercase=${PASSWORD_REQUIRE_UPPERCASE:true}
app.password.require-lowercase=${PASSWORD_REQUIRE_LOWERCASE:true}
app.password.require-digit=${PASSWORD_REQUIRE_DIGIT:true}
app.password.require-special=${PASSWORD_REQUIRE_SPECIAL:true}

# Session security
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=${COOKIE_SECURE:false}
server.servlet.session.cookie.same-site=strict

# ======================
# === EMAIL CONFIG   ===
# ======================
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=imanecompte024@gmail.com
spring.mail.password=eeor guik iftz nico
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ======================
# === OAUTH2 CONFIG  ===
# ======================
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=openid,email,profile
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/google
spring.security.oauth2.client.registration.google.client-name=Google
spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/v2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v3/userinfo
spring.security.oauth2.client.provider.google.user-name-attribute=sub

# ======================
# === HTTPS CONFIG   ===
# ======================
server.ssl.enabled=${SSL_ENABLED:false}
server.ssl.key-store=${SSL_KEYSTORE:classpath:keystore.p12}
server.ssl.key-store-password=${SSL_KEYSTORE_PASSWORD}
server.ssl.key-store-type=PKCS12

# ======================
# === FRONTEND URL   ===
# ======================
app.frontend.url=${FRONTEND_URL:http://localhost:3000}

# ======================
# === LOGGING        ===
# ======================
logging.level.com.dira.diravenir1.security=DEBUG
logging.level.org.springframework.security=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# ======================
# === JWT CONFIG     ===
# ======================
app.jwt.secret=your-super-secure-jwt-secret-key-256-bits-minimum-required-for-hs512-algorithm-make-it-very-long-and-random
# ======================
# === AUTRES         ===
# ======================
server.port=${SERVER_PORT:8084}
